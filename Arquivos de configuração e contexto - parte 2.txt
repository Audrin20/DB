select * from pg_settings where name in ('config_file'); /* localiza os arquivos */
select distinct(context) from pg_settings; /* contexto internal: Não podem ser modificadas, só no processo de compilação do pos
select pg_show_all_settings(); Função do pg_settings (que é uma view) */

select * from pg_settings where name in ('port','max_connections','shared_buffers');
max_connections = 100
port = 5432
shared_buffers = 16384